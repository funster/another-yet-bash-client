<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.aim.anotheryetbashclient">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".BashApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="ru.aim.anotheryetbashclient.MainActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="ru.aim.anotheryetbashclient.helper.QuoteService"
            android:exported="false" />

        <activity
            android:name="ru.aim.anotheryetbashclient.SettingsActivity"
            android:label="@string/title_activity_settings" />

        <provider
            android:name="ru.aim.anotheryetbashclient.QuotesContentProvider"
            android:authorities="quotes"
            android:enabled="true"
            android:exported="false" />

    </application>

</manifest>
